# Iterative search starting with a random point and finding the nearest point at least (2*radius) away from it that isn't already selected.
# Brute force method

# Start with random point
randompt <- sample(length(bbs_aea), 1)



spDistsN1()